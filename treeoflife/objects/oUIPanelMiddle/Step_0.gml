/// @description 

// Inherit the parent event
event_inherited();

if (title == noone && icon != noone) 
	title = NewHighlight(
				icon,
				x - 3,
				y + border,
				width,
				"Help");               